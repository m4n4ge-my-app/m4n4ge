import BaseLayer from '../../components/baseLayer/BaseLayer';
import GetAssistGrid from '../../components/grids/GetAssistGrid';

const Archives = () => {
  return (
    <BaseLayer type="archive">
      <GetAssistGrid formLabel="Archives" />
    </BaseLayer>
  );
};

export default Archives;
