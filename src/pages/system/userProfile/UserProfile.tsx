import BaseLayer from '../../../components/baseLayer/BaseLayer';
import GetAssistGrid from '../../../components/grids/GetAssistGrid';

const UserProfile = () => {
  return (
    <BaseLayer type="profile">
      <GetAssistGrid formLabel="User Profile" />
    </BaseLayer>
  );
};

export default UserProfile;
